name: Build MegaSelfCodeLoader
on:
  workflow_dispatch:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2
      - name: Restore NuGet packages (packages already vendored in repo, this is a safety net)
        run: nuget restore MegaSelfCode1.21.4/MegaSelfCodeLoader.slnx
        continue-on-error: true
      - name: Build (Release x64)
        run: >
          msbuild MegaSelfCode1.21.4/MegaSelfCodeLoader.slnx
          /p:Configuration=Release
          /p:Platform=x64
          /p:PlatformToolset=v143
          /m
      - name: Ensure WebView2Loader.dll is in output (safety net)
        run: copy "MegaSelfCode1.21.4\packages\Microsoft.Web.WebView2.1.0.3719.77\build\native\x64\WebView2Loader.dll" "MegaSelfCode1.21.4\x64\Release\WebView2Loader.dll"
        shell: cmd
        continue-on-error: true
      - name: Upload exe
        uses: actions/upload-artifact@v4
        with:
          name: MegaSelfCodeLoader
          path: MegaSelfCode1.21.4/x64/Release/
